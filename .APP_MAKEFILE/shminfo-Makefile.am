#!/bin/make
app="@alsy.app.name"
INSTALLDIR=${GTK3_PREFIX}
compile:
	cd ../build/${app} && ninja
install:
	cd ../build/${app} && ninja install
	cd ../build/${app} && ninja DESTDIR=${INSTALLDIR} install
clean:
	rm -rfd ../build/${app}
